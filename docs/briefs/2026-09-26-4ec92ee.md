---
from: 694342b
to: 4ec92ee
merges: [115, 116, 117, 118, 119, 120, 121, 122, 123, 124, 127, 128, 131]
---

# Brief — 2026-09-26, 694342b..4ec92ee

## What landed
- **#115 — 0077's generator-only A/B, measured**: 4/30 invented under the old prompt, 0/30 under the new; fills in what #114 shipped planned-but-not-run.
- **#116 — scribe brief for 8326b98..694342b**: no product change.
- **#117–#120, #122 — Tests by purpose (closes #110), 5 steps**: shared builders in `tests/support.py`, the document/chat/service/API/CLI/TUI/prompt clusters regrouped by rule; suite 627k → ~530k characters across the run, 704 → ~680 cases, 0 cross-test imports.
- **#121 — The scribe runs every ~10 merges, not every ~3**.
- **#123 — Three timing-dependent tests wait on the state they are about** (closes #113): test-side fixes, no decision record (bug fix, not a decision).
- **#124 — A direct reply asked for as a file is written as that document** (closes #80): `DirectResponder` gets the delivered-files note and `DIRECT_DOCUMENT_RULE`; request stays on the direct route.
- **#127 — An answer written to a file is that file, not a text about it** (closes #126): `ANSWER_FILE_RULE`; file runs writing about their own file 8/16 → 2/16.
- **#128 — document_intent reads "save it to a file" as a document asked for** (closes #125): `FILE_REQUEST_RULE`.
- **#131 — Effort is proportionate to the risk** (closes #130): a per-issue time budget in `CLAUDE.md`, `evals/probe.py` for node-level measurement.

## What was decided, and why
Every PR above that took a decision wrote its own record in the same PR (0077 update by #115; 0110, 0121, 0080, 0126, 0125, 0130 each by their own PR) and kept the index current — nothing to reconstruct this pass.

## Left open
- **0077's A/B is now run** (was the previous brief's open item): 4/30 → 0/30, one-sided Fisher p ≈ 0.06 — small n, but every hit restated the removed bullet word for word.
- **#5** — no interjection on an announced plan: still open, unchanged since the last brief.

## What to watch
- **0110's five steps are all in**: `tests/test_default_pack.py`'s prompt rules are now constants (`CAVEATS_RULE`, `BRIEF_RULE`, `UNREADABLE_RULE`); a rule dropped from its prompt without updating the constant now fails its own test, not a wording match.
- **0126 partially supersedes 0077**: `ANSWER_FILE_RULE` only fires when the delivered-files list names the answer itself; a profile that never routes through that list (a hand-built `AgentContext`, not `build_app`) does not get it.
- **#123's fixes are unfalsified on 2 of 3 tests** (did not reproduce locally, fixed on diagnosis of a wall-clock window) — watch `test_an_owner_that_lost_its_lease_stops_without_writing` and `test_the_screen_follows_a_job_while_it_runs` for a recurrence under CI load.

## Stale claims fixed
- `docs/decisions/0110-tests-by-purpose.md` and its index line: "accepted — first step: the shared module and the document cluster" → "accepted — all 5 steps landed (#117–#120, #122)" — status only, all 5 steps closed #110 in this range.
- `CLAUDE.md`: none (all PRs in range added or updated their own rule line correctly). Budget held: 38 974 → 37 707 characters (see PR for the condensed list; `scripts/check_moved.py origin/main`: 0 missing, 0 lost references, 17 condensed).
